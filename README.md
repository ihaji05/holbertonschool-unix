# Shell Basics

## 0- bring_me_home

Script to change directory to the user's home folder

#Shell Redirection

## 0-hellofile 
Displays of the content of the /etc/passwd file.

# Shell Redirections

## 0-hellofile
Script that displays the entire content of the /etc/passwd file.

## 1-lastlines
Script that displays the last 10 lines of the /etc/passwd file.

## 2-firstlines
Script that displays the first 10 lines of the /etc/passwd file.

## 3-third_line
Script that displays the third line of the file iacta.

## 4-cwd_state
Script that writes the result of 'ls -la' into a file named ls_cwd_content, overwriting if it exists.
